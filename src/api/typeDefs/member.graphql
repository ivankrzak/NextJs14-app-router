type Member {
  id: Int
  firstName: String
  lastName: String
  address: String
  email: String
  telNumber: String
  isStudent: Boolean
  memberships: [Membership]
  createdAt: Date
  updatedAt: Date
}

type Query {
  members: [Member]!
}
